/*Calculadora Científica
🎯 Enunciado:
Crea una calculadora que permita calcular seno, coseno, tangente, raíz cuadrada y potencia.
*/

function calcularOperacion() {
    // COMPLETAR: Realizar el cálculo según la operación seleccionada
}


if (typeof document !== 'undefined') {
    document.addEventListener("DOMContentLoaded", () => {
        // Completar: Asignar el evento
    });
}

module.exports = { calcularOperacion };
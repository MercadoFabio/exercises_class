/*Chat en Tiempo Real con LocalStorage
🎯 Enunciado:
Crea un chat en tiempo real donde los mensajes se almacenen en localStorage.

Debe haber una entrada de texto y un botón de enviar.

Al recargar la página, los mensajes deben mantenerse.
*/

function enviarMensaje() {
    // COMPLETAR: Guardar mensajes en localStorage y mostrarlos en el chat
}

if (typeof document !== 'undefined') {
    document.addEventListener("DOMContentLoaded", () => {
        // Completar: Asignar el evento
    });
}

module.exports = { enviarMensaje };
/*Galería de Imágenes con Fetch API
🎯 Enunciado:
Crea una galería de imágenes con la API https://picsum.photos/v2/list?page=1&limit=5.

Al hacer clic en "Cargar Imágenes", se deben mostrar 5 imágenes aleatorias.

Debe haber un botón para recargar nuevas imágenes.
*/

async function cargarImagenes() {
    const galeria = document.getElementById("galeria");

    // COMPLETAR: Llamar a la API y mostrar imágenes
}


if (typeof document !== 'undefined') {
    document.addEventListener("DOMContentLoaded", () => {
        // Completar: Asignar el evento
    });
}

module.exports = { cargarImagenes };